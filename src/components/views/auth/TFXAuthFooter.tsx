import React, { ReactElement } from "react";

console.log("Loaded TFXAuthFooter");

const AuthFooter = (): ReactElement => {
    return <footer className="mx_AuthFooter" role="contentinfo" />;
};

export default AuthFooter;
